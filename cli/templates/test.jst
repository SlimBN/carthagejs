'use strict';

const Carthage = require('carthage');

class {{= data.name }} extends Carthage.mocha.Test {

  test(expect) {

    it('Should do something', done => {

      expect(null).to.not.exist;
      done();

    });

  }

}

module.exports = {{= data.name }};
